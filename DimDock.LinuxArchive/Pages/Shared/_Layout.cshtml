<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    @if(@ViewBag.UserScalable)
    {
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    }
    else
    {
        <meta name="viewport" content="width=device-width, user-scalable=no">
    }
    <title>@ViewData["Title"] - RicyArt</title>
    <link rel="stylesheet" href="~/gallery/css/dimdock.css" />
</head>
<body>
    <header class="smallText">
        @if(@ViewBag.ShowNavigation)
        {
        <div style="padding:10px;">
            <a href="/gallery">Home</a>
            @if (ViewBag.SketchNavigation)
            {
                @("|")
                <a href="~/gallery/Sketch/">Sketch Archive Home</a>
                <!--
        @("|")
        <a href="~/gallery/Sketch/Custom/">Custom</a>
        @("|")
        <a href="~/gallery/Sketch/About/">About</a>
            -->
            }
            else
            {
                @("|")
                <a href="~/gallery/Sketch/">Sketch Archive (NSFW)</a>
            }
        </div>
        }
    </header>

    <div>
        <main role="main" class="container">
            @RenderBody()
        </main>
    </div>

    <footer>
    </footer>

</body>
</html>
